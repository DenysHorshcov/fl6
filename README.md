# Практичне завдання 6
# Завдання
З використанням С-інтерфейсу до Clang для одинокого cpp-файлу з програмою
мовою С++ визначити таке.
Варіант 7: Кількість анонімних функцій, що захоплюють контекст за посиланням.
# Як запустити програму

Програма рахує, скільки в одному `*.cpp`-файлі є лямбда-виразів, що захоплюють контекст за посиланням (`&` у списку захоплень).

## Варіант 1: через `count_ref_lambdas.exe`

1. Відкрити консоль у папці проєкту.
2. Запустити команду:

   ```bat
   count_ref_lambdas.exe path\to\file.cpp -std=c++17

## Варіант 1: через run_checker.bat
Перетягнути потрібний *.cpp-файл мишкою на run_checker.bat.
У вікні консолі з’явиться результат
